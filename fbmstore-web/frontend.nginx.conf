server {
    listen 80;
    root /usr/share/nginx/html/fbmstore;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location ~* \.(ico|css|js|gif|jpe?g|png|woff2?|eot|ttf|svg|otf|webp)$ {
        expires 6M;
        access_log off;
        add_header Cache-Control "public";
    }
}